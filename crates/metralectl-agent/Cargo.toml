# SPDX-License-Identifier: AGPL-3.0-only
[package]
name = "metralectl-agent"
description = "Local agent that lets the Metrale Engine website launch recipes on this machine."
version = "0.1.1"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
metralectl-core.workspace = true
metralectl-protocol.workspace = true
anyhow.workspace = true
serde.workspace = true
serde_json = "1"
serde_yaml_ng.workspace = true
base64 = "0.22"
axum = { version = "0.8", features = ["ws"] }
tokio = { version = "1", features = ["rt-multi-thread", "macros", "net", "signal", "sync", "process", "fs", "io-util", "time"] }
subtle = "2"
thiserror.workspace = true
ed25519-dalek = { version = "3", default-features = false, features = ["pkcs8", "alloc"] }
sha2.workspace = true
hex = "0.4"
zeroize = "1"
getrandom = "0.3"
mdns-sd = { version = "0.21", default-features = false }
rustls = { version = "0.23", default-features = false, features = ["ring", "std", "tls12"] }
tokio-rustls = { version = "0.26", default-features = false, features = ["ring", "tls12"] }
rcgen = { version = "0.14", default-features = false, features = ["pem", "crypto", "ring"] }
spake2 = "0.4"
rustls-pki-types = "1.15.1"

[dev-dependencies]
# The recording mocks are feature-gated so they never ship in a release build.
metralectl-core = { workspace = true, features = ["test-mocks"] }

[features]
test-mocks = []

[lints]
workspace = true

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[target.'cfg(windows)'.dependencies]
windows-sys = { version = "0.59", features = [
  "Win32_Foundation",
  "Win32_Storage_FileSystem",
  "Win32_System_SystemInformation",
] }
